
import { Hash } from "crypto";

const hashing = {

    hashData: function (data) {

        if (!data) {
            return []
        }
        const hashedData = hash(data).toString();
        return hashedData;
    },


    hashData: function (data) {

        if (!data) {
            return []
        }
        const hashedData = hash(data).toString();
        return hashedData;
    }



}

export default hashing;